import 'package:flutter/material.dart';

class Safetymap extends StatelessWidget {
  const Safetymap({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("safety map"),
    );
  }
}